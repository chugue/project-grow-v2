
<aside class="px-5" style="text-align: center; flex: 0 0 auto; width: 300px;">
    <div class="mb-3">
        <div class="fs-4">기업 홈</div>
    </div>
    <div class="card">
        <div style="width: 100%;">
            <div class="relative mb-3">
                <div>
                    <a href="/comp/profile-update-form"
                       onclick="window.open(this.href, '_blank', 'width=1200,height=900,toolbars=no,scrollbars=no, resizable=no'); return false;">
                        <div class="user-img">
                            <img src="/upload/{{sessionComp.imgFileName}}" class="rounded" alt="Cinque Terre"
                                 style="max-width: 80px;">
                        </div>
                    </a>
                </div>
            </div>
            <div>
                <h6 class="card-subtitle text-muted">카카오</h6>
                <hr>
                <ul class="nav flex-column nav-pills">
                    <li class="nav-item">
                        <a class="nav-link sub-menu a p-1 mb-1" href="/comp/{{sessionComp.id}}/comp-home">공고 관리</a>
                    </li>
                    <li class="nav-item">
                        <a id="apply" class="nav-link sub-menu a p-1 mb-1" href="/comp/{{sessionComp.id}}/apply">지원 / 제안</a>
                    </li>

                    <hr>
                    <li class="nav-item">
                        <a href="/comp/{{sessionComp.id}}/update-form" class="nav-link sub-menu a p-1 mb-1">정보수정</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link  sub-menu a p-1" href="/">로그아웃</a>
                    </li>
                </ul>
            </div>
        </div>
    </div>
</aside>
<script>
    $(document).ready(function () {
        // 현재 페이지의 URL을 가져오는 함수
        function getCurrentPageUrl() {
            return window.location.pathname;
        }

        // 현재 페이지에 대한 메뉴 항목을 활성화하는 함수
        function activateMenu() {
            var currentPageUrl = getCurrentPageUrl();
            $('aside .sub-menu').each(function () {
                if ($(this).attr('href') === currentPageUrl) {
                    $(this).addClass('btn-mint');
                }
            });
        }

        // 페이지가 로드될 때 메뉴 항목 활성화 실행
        activateMenu();
    });
</script>